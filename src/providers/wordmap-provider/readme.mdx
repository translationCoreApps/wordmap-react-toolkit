---
name: WordmapProvider
---

import { Props } from 'docz';
import {WordmapProvider} from './WordmapProvider';

# WordmapProvider

This component provides a `wordMAP` prop to all of it's children.

<Props of={WordmapProvider}/>

## Basic usage

```jsx
<WordmapProvider>
    <MyComponent/>
</WordmapProvider>
```